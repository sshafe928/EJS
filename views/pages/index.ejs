<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include("../partials/head"); %>
    <title>EJS HOME-PAGE</title>
</head>
<body classname="container">
    <header>
        <!-- include ejs header -->
        <%- include('../partials/header',{variant: 'compact'}); %>
    </header>

    <main>
        <div className="jumbotron">
            <h1>This is pretty awesome</h1>
            <p>welcome to using template js</p>
            <!-- NEw: EJS + JS -->
            <h2>using variables</h2>
            <p> this is my fervent <%= tagline %></p>
            <ul>
                <% people.forEach(function(person) { %>
                    <li>
                        <strong><%= person.first_name+ " " +person.last_name %></strong></br>
                        <em>Email:</em> <%= person.email %> </br>
                        <em>Gender:</em> <%= person.gender %> </br>
                        <em>DOB:</em> <%= person.DOB %> </br>

                    </li>
                    <!-- closing ejs tag -->
                    <% }); %>
            </ul>
        </div>
    </main>
    <footer>
        <% include("../partials/footer"); %>
    </footer>
</body>
</html>